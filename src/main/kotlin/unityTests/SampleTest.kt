package unityTests



/*
class SampleTest {
    private val actualNumber = 30

    fun testTheNumber() {
        assertEquals(
            expected = 30,
            actual = actualNumber,
            message = "Incorrect Number"
        )
    }

    @Test
    fun testDifferentsNumbers() {
        assertFalse(
            actual = numbersAreDifferent(actualNumber, expected = 1),
            message = "Numbers are the same"
        )
    }

    @Test
    fun calculatePassZeroAssertAnError() {
        assertFails {
            calculate(number1 = 100, number2 = 0)

        }
    }

    @Test
    fun `calculate PassZeroAssertAnCorrectResult`() {
        assertIs<Int>(value = calculate(number1 = 100, number2 = 10))
    }
}


fun calculate(number1: Int, number2: Int) = number1 / number2
fun numbersAreDifferent(actual: Int, expected: Int) = actual == expected*/
